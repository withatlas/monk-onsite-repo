# Your interviewer will provide the real DATABASE_URL.
# Do not commit .env.local.
DATABASE_URL=postgresql://postgres.<project-ref>:<db-password>@<pooler-host>:5432/postgres

# Optional: required only if you intentionally reset/reseed the shared remote DB.
# REMOTE_RESET_CONFIRM=monk-onsite-repo
